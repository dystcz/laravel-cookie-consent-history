<?php

use Dystcz\CookieConsentHistory\Actions\SaveCookieConsent;
use Dystcz\CookieConsentHistory\Data\CookieConsentData;
use Illuminate\Support\Str;

it('can save cookie consent', function () {
    // $action = new SaveCookieConsent();
    //
    // $cookie_id = Str::random();
    // $data = [
    //     'level' => ['necessary'],
    //     'revision' => 0,
    //     'data' => null,
    //     'rfc_cookie' => false,
    // ];
    //
    // $action->handle(new CookieConsentData($cookie_id, $data));
    //
    // $this->assertDatabaseHas('cookie_consents', [
    //     'cookie_id' => Str::random(),
    //     'data' => $data,
    // ]);

    $this->assertTrue(true);
});
